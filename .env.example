# =======================================================================
# POLYMARKET MARKET MAKER BOT — EXAMPLE ENV
# Copy to .env and fill values. Unset values use sensible defaults.
# =======================================================================

# ------------------------
# MODE
# ------------------------
# mode: dry_run | paper | live
MODE=dry_run
# data_source: real | sim  (real uses live Polymarket+Binance feeds; sim uses synthetic)
DATA_SOURCE=real
# LIVE_API_ENABLED must be explicitly "true" to allow live order placement.
LIVE_API_ENABLED=false

# ------------------------
# BANKROLL / SIZING
# ------------------------
BANKROLL_USDC=500
MIN_ORDER_SIZE_USDC=5
MAX_ORDER_SIZE_USDC=50
DEFAULT_QUOTE_SIZE_USDC=15

# ------------------------
# ASSET / DISCOVERY
# ------------------------
# Comma-separated list; bot will pick the best active contract matching these.
TARGET_ASSETS=BTC,ETH,SOL,DOGE,XRP,BNB,HYPE
# Comma-separated; supported tags: 5m,15m,1h,daily
TARGET_INTERVALS=5m,15m
DISCOVERY_POLL_MS=30000
DISCOVERY_MIN_LIQUIDITY_SCORE=0.15
DISCOVERY_PREWARM_SECS=60

# ------------------------
# POLYMARKET FEED
# ------------------------
POLYMARKET_HTTP=https://clob.polymarket.com
POLYMARKET_WS=wss://ws-subscriptions-clob.polymarket.com/ws/market
POLYMARKET_GAMMA=https://gamma-api.polymarket.com
POLY_FEED_STALE_MS=7000
POLY_RECONNECT_MIN_MS=1000
POLY_RECONNECT_MAX_MS=30000

# ------------------------
# BINANCE FEED
# ------------------------
BINANCE_WS=wss://stream.binance.com:9443/stream
BINANCE_ENABLED=true
BINANCE_FEED_STALE_MS=4000
BINANCE_SYMBOL_MAP=BTC:BTCUSDT,ETH:ETHUSDT,SOL:SOLUSDT,DOGE:DOGEUSDT,XRP:XRPUSDT,BNB:BNBUSDT,HYPE:HYPEUSDT

# ------------------------
# AVELLANEDA-STOIKOV
# ------------------------
AV_GAMMA=0.8
AV_K=1.5
AV_SIGMA_FLOOR=0.004
AV_SPREAD_MIN_BPS=40
AV_SPREAD_MAX_BPS=400
AV_MIN_EDGE_BPS=18

# ------------------------
# SIGNAL WEIGHTS
# ------------------------
W_MICROPRICE=0.45
W_POLY_IMBALANCE=0.15
W_BINANCE_IMBALANCE=0.20
W_BINANCE_AGGRESSOR=0.10
W_BINANCE_MOMENTUM=0.10

# ------------------------
# PAPER SIMULATION
# ------------------------
PAPER_FILL_PROB_BASE=0.22
PAPER_PARTIAL_FILL_PROB=0.3
PAPER_MAKER_FEE_BPS=0
PAPER_TAKER_FEE_BPS=20
PAPER_LATENCY_MS=140
PAPER_TOXICITY_BASELINE=0.18
PAPER_SLIPPAGE_BPS=4

# ------------------------
# RISK
# ------------------------
RISK_SESSION_LOSS_CAP_USDC=100
RISK_DRAWDOWN_CAP_USDC=60
RISK_CONSECUTIVE_LOSS_CAP=6
RISK_API_ERROR_CAP=25
RISK_TOXIC_FLOW_EMA_CAP=0.55
RISK_MAX_INVENTORY_USDC=200

# ------------------------
# ADVERSE SELECTION
# ------------------------
ADV_SAMPLE_1S_MS=1000
ADV_SAMPLE_5S_MS=5000
ADV_EMA_ALPHA=0.08

# ------------------------
# AUTO-HEALING
# ------------------------
AUTOHEAL_INTERVAL_MS=180000
AUTOHEAL_MIN_FILLS=8
AUTOHEAL_SPREAD_STEP_BPS=5
AUTOHEAL_SIZE_STEP=0.1

# ------------------------
# EXECUTION / RATE LIMIT
# ------------------------
ORDER_MAX_AGE_MS=15000
ORDER_REPLACE_DRIFT_BPS=18
ORDER_POST_ONLY=true
RATE_LIMIT_ORDERS_PER_SEC=4
RATE_LIMIT_BURST=8

# ------------------------
# LIVE AUTH (Polymarket CLOB)
# ------------------------
# Fill these ONLY when going live. Never commit real values.
POLY_PRIVATE_KEY=
POLY_FUNDER_ADDRESS=
POLY_API_KEY=
POLY_API_SECRET=
POLY_API_PASSPHRASE=
POLY_CHAIN_ID=137

# ------------------------
# DASHBOARD / TELEMETRY
# ------------------------
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=8787
LOG_LEVEL=info

# ------------------------
# ALERTS
# ------------------------
DISCORD_WEBHOOK_URL=
